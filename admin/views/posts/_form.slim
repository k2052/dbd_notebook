.group
  ==f.label :slug
  ==f.error_message_on :slug
  ==f.text_field :slug, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :title
  ==f.error_message_on :title
  ==f.text_field :title, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :_type
  ==f.error_message_on :_type
  ==f.text_field :_type, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :tags
  ==f.error_message_on :tags
  ==f.text_field :tags, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :comment_count
  ==f.error_message_on :comment_count
  ==f.text_field :comment_count, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :status
  ==f.error_message_on :status
  ==f.text_field :status, :class => :text_field
  span.description Ex: a simple text

.group.navform.wat-cf
  ==f.submit pat(:save), :class => :button
  ==f.submit pat(:cancel), :onclick => "window.location='#{url(:posts, :index)}';return false", :class => :button

.group
  ==f.label :title
  ==f.error_message_on :title
  ==f.text_field :title, :class => :text_field
  span.description Ex: a simple text
  
.group
  ==f.label :embed
  ==f.error_message_on :embed
  ==f.text_field :embed, :class => :text_field
  span.description Ex: a simple text
  
.group
  ==f.label :artist
  ==f.error_message_on :artist
  ==f.text_field :artist, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :track_num
  ==f.error_message_on :track_num
  ==f.text_field :track_num, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :song_title
  ==f.error_message_on :song_title
  ==f.text_field :song_title, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :tags
  ==f.error_message_on :tags
  ==f.text_field :tags, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :album
  ==f.error_message_on :album
  ==f.text_field :album, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :year
  ==f.error_message_on :year
  ==f.text_field :year, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :length
  ==f.error_message_on :length
  ==f.text_field :length, :class => :text_field
  span.description Ex: a simple text

.group
  ==f.label :status
  ==f.error_message_on :status
  ==f.text_field :status, :class => :text_field
  span.description Ex: a simple text

.group.navform.wat-cf
  ==f.submit pat(:save), :class => :button
  ==f.submit pat(:cancel), :onclick => "window.location='#{url(:audios, :index)}';return false", :class => :button
